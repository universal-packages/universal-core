import { runApp } from "@universal-packages/core";

describe("ExampleApp", () => {
  it("works", async () => {
    const stopApp = await runApp("example-app", { exitType: "throw" });

    await stopApp();
  });
});
